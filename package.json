{
  "scripts": {
    "tdd": "jest --watch --config=jest.config.unit.json",
    "test": "jest --forceExit --config=jest.config.unit.json",
    "serve-production": "NODE_ENV=production sls webpack serve",
    "serve": "sls webpack serve",
    "deploy": "sls deploy -v"
  },
  "dependencies": {
    "@types/aws-lambda": "^0.0.16",
    "@types/jest": "20.0.2",
    "@types/node": "^8.0.24",
    "jest": "^20.0.4",
    "rimraf": "^2.6.1",
    "serverless-offline": "^3.15.3",
    "serverless-webpack": "^2.2.0",
    "ts-jest": "20.0.7",
    "ts-loader": "^2.3.3",
    "typescript": "^2.4.1",
    "webpack": "^3.5.5"
  }
}
